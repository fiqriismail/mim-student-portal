# 21-08-2026

- Drafted `brain/docs/ARCHITECTURE.md` from the approved PRD (`brain/docs/PRD.md`).
- Stack confirmed with product owner: Next.js 16 frontend, ASP.NET Core 10 (Minimal APIs) backend with Clean Architecture, PostgreSQL + EF Core. Hosting left open (AD-1).
- Key design calls: Next.js as a session-owning BFF (browser never talks to the API directly); vertical-slice organisation inside the Application layer, one folder per PRD user story; row-level locking + unique partial index for the atomic seat-capacity guarantee (AC-4.1.4).
- Open architecture decisions logged in §11 of the doc (hosting, mediator library, background job runner, Identity implementation).
- Converted the ASCII system-context diagram (§3) to a Mermaid graph for better rendering.
- Populated `brain/README.md` as the root navigation index (was empty), listing PRD, ARCHITECTURE, and daily-notes.
- Fixed `brain/design-system/base.css`: the file was bare custom-property declarations with no `:root` wrapper (invalid CSS). Restructured into brand palette, shadcn-style semantic tokens (light theme), a Tailwind v4 `@theme inline` mapping, and added Roboto (Google Fonts) as the system font.
- Created `brain/design-system/guide.html` — a two-column (sidebar nav + content) visual guide covering colors, typography, radius/elevation, buttons, badges, forms, cards, alerts, avatars, tabs and tables, styled with Tailwind CDN configured against the `base.css` tokens.
- Updated `brain/README.md` with a new "Design system" section per the standing instruction to keep it current.
- Noted `brain/design-system/mokups/MIM Student Portal Mockups.dc.html` in `brain/README.md` — interactive Phase 1 screen mockups (Home, Course detail, Register, Check email, Login, Dashboard) with desktop/mobile toggle, styled inline with the same Roboto + Oxford Navy palette as `base.css`.
- Initialized git, added `.gitignore` (Node/pnpm/.NET/env/editor), made the initial commit (PRD, architecture doc, design system, mockups), and pushed to `origin/main` at https://github.com/fiqriismail/mim-student-portal.git. Monorepo app scaffolding (`apps/web`, `apps/api`) intentionally deferred — to be done step by step, not auto-run.
- Generated Phase 1 delivery stories under `brain/stories/`: 23 backend (`BE-*`) and 18 frontend (`FE-*`), one per PRD user story per layer it touches (most stories pair FE+BE; notifications/seeding/export/role-model/audit-logging are backend-only, analytics instrumentation is frontend-only). Every story has frontmatter (id, title, layer, epic, prd_ref, priority, status, depends_on) and links to its dependencies and its cross-layer counterpart. Added `backend/STORYBOARD.md` and `frontend/STORYBOARD.md` grouping stories by epic, plus `stories/README.md` explaining the conventions. Linked all of it from `brain/README.md`.
- Added an `E0 — Project Setup` epic ahead of the PRD epics: [BE-0.1](../stories/backend/BE-0.1.md) scaffolds the ASP.NET Core solution and connects it to the developer's local PostgreSQL instance; [FE-0.1](../stories/frontend/FE-0.1.md) scaffolds the pnpm/Turborepo workspace and the Next.js app, wiring in `base.css` and shadcn/ui. Both have `prd_ref: N/A` (infra, not a PRD story) and no dependencies — every other story sits on top of them. Storyboards, `stories/README.md` and `brain/README.md` counts updated to 24/19.
- Implemented [BE-0.1](../stories/backend/BE-0.1.md) on branch `feature/be-0.1-scaffold-backend`. Resolved a path discrepancy first: ARCHITECTURE.md §4.1/§5.1 said `src/backend`/`src/frontend`, but the repo already had an empty `apps/` folder from the Turborepo convention noted in this file's earlier entry — confirmed with the product owner to standardise on `apps/api` and `apps/web`, and updated ARCHITECTURE.md accordingly. Scaffolded the ASP.NET Core 10 solution at `apps/api/`: `MIM.Portal.Domain` → `MIM.Portal.Application` → `MIM.Portal.Infrastructure` → `MIM.Portal.Api`, referenced per architecture §4.1's dependency direction (Domain has zero references). Added EF Core + Npgsql to Infrastructure, `PortalDbContext` registered against a connection string stored via `dotnet user-secrets` (never committed) pointing at the local Postgres instance (`localhost:5432/student_portal_db`, `postgres` user — trust auth locally, no password needed). Created and applied an initial empty migration, confirmed with `psql` against `__EFMigrationsHistory`. Added a `GET /health` endpoint, verified 200 with the app running. Pre-created the empty vertical-slice folder structure in `Application/` (one folder per epic, one subfolder per story, per architecture §4.2) with `.gitkeep` files so `BE-1.1` onward has somewhere to land. Updated BE-0.1's status to `done` in its frontmatter and in `backend/STORYBOARD.md`. Pushed and opened [PR #1](https://github.com/fiqriismail/mim-student-portal/pull/1), merged to `main`.
- Implemented [FE-0.1](../stories/frontend/FE-0.1.md) on branch `feature/fe-0.1-scaffold-frontend`. Created the root pnpm/Turborepo workspace (`pnpm-workspace.yaml` scoping `apps/*`, root `package.json`, `turbo.json` with `dev`/`build`/`lint` tasks) — `apps/api` isn't picked up since it has no `package.json`, exactly as the story expects. Scaffolded Next.js 16 in `apps/web` via `create-next-app` (App Router, TypeScript, ESLint, Tailwind v4, pnpm), then `shadcn@latest init` (Radix base, Nova preset) which wired Tailwind v4 + `components.json` + `components/ui/button.tsx`. Replaced the default shadcn/Geist tokens in `app/globals.css` with `brain/design-system/base.css`'s brand palette, semantic tokens, and Roboto import verbatim (kept shadcn's extra sidebar/chart token slots, pointed them at the brand scale) — this is the first place those Tailwind v4 `@theme`/`@import` directives actually build. Dropped the unused Geist font import from `layout.tsx` in favour of the CSS-based Roboto import. Built a placeholder home route (`app/page.tsx`) using the shadcn `Button` plus brand-toned heading/muted text, proving the token pipeline end-to-end — verified visually via `pnpm dev` (curled `/`, confirmed Roboto + `text-primary` in the rendered HTML) and confirmed `pnpm lint` / `pnpm build` both pass clean. Updated FE-0.1's status to `done` in its frontmatter and in `frontend/STORYBOARD.md`.
